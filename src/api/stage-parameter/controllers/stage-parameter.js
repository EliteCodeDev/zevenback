'use strict';

/**
 * stage-parameter controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::stage-parameter.stage-parameter');
